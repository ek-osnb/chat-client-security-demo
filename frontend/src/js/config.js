const BASE = "";

export const API_CONFIG = {
    BASE_URL: BASE,
    ENDPOINTS: {
        LOGIN: BASE+"/api/login",
        LOGOUT: BASE+"/api/logout",
        USER: BASE+"/api/user",
        PROMPT: BASE+"/api/prompt"
    }
};